#pragma once

#include "../../ICommand.hpp"

class DeleteEntityCommand : public ICommand {
    public:
        DeleteEntityCommand() = default;
        ~DeleteEntityCommand() = default;

    private:
};
